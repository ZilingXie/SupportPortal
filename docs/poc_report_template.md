# SupportPortal POC 报告模板

## 1. 摘要
- 项目名称：SupportPortal
- 报告日期：`YYYY-MM-DD`
- 作者/团队：`<填写>`
- POC 结论：`通过 / 部分通过 / 未通过`

简述（3-5 句）：
- 背景：`<填写>`
- 方案：`<填写>`
- 结果：`<填写>`
- 建议：`<填写>`

## 2. 背景与问题定义
- 当前业务痛点：`<填写>`
- 目标用户：`客户端 / 工程师 / 管理员`
- 需要验证的关键假设：
  1. `<填写>`
  2. `<填写>`
  3. `<填写>`

## 3. POC 目标与范围
### 3.1 目标
1. 验证工单闭环：客户提问 -> AI/RAG -> 工程师介入 -> 关闭工单。
2. 验证三端协作可用性与实时性。
3. 验证单机部署可运行并可演进到高并发架构。

### 3.2 范围内
- 单机架构：`Nginx + API + WS Gateway + Worker + Redis + PostgreSQL`
- 三端页面：`/client/`、`/engineer/`、`/dashboard/`
- 异步任务链路与实时事件推送

### 3.3 范围外
- 多节点容灾
- 全量压测与正式生产上线
- 完整运营后台能力

## 4. 方案概述
### 4.1 业务流程
1. 客户提交问题 -> 创建/更新工单。
2. 情感识别（高风险直接人工介入）。
3. Agora 相关性判断（非相关拒答）。
4. 相关问题走 RAG（命中自动回复，未命中升级工程师）。
5. 工程师托管/接管处理并关闭工单。

### 4.2 架构设计
- 前端入口：Nginx
- 业务接口：FastAPI API
- 实时推送：WS Gateway
- 异步处理：Worker
- 消息与队列：Redis
- 数据存储：PostgreSQL（可扩展 pgvector）

> 架构图引用：`docs/support_system_architecture.md`

## 5. 实施与部署
### 5.1 本地环境（Podman）
- 访问地址：
  - Client: `http://localhost:8080/client/`
  - Engineer: `http://localhost:8080/engineer/`
  - Dashboard: `http://localhost:8080/dashboard/`
  - Health: `http://localhost:8080/health`

### 5.2 EC2 环境（Docker）
- 域名：`support.stellarix.space`
- 安全组：`22/80/443`
- 部署方式：`docker compose`

> 部署步骤引用：`docs/deploy_single_host_ec2.md`

## 6. 验证设计
### 6.1 测试场景
1. 普通问题（RAG 命中）
2. RAG 未命中（工程师介入）
3. 高情绪问题（直接人工介入）
4. 工程师托管与接管切换
5. 管理端追踪工单状态与事件

### 6.2 验收指标（示例）
| 指标 | 目标 | 实测 | 结论 |
|---|---:|---:|---|
| API 响应（P95） | `<填写>` | `<填写>` | `<填写>` |
| 告警推送延迟 | `<= 30s` | `<填写>` | `<填写>` |
| 路由可访问性 | `100%` | `<填写>` | `<填写>` |
| RAG 命中准确率 | `>= 85%` | `<填写>` | `<填写>` |

## 7. 结果与发现
### 7.1 已验证能力
1. `<填写>`
2. `<填写>`
3. `<填写>`

### 7.2 问题与限制
1. `<填写>`
2. `<填写>`
3. `<填写>`

## 8. 风险评估
| 风险 | 影响 | 可能性 | 缓解措施 |
|---|---|---|---|
| `<填写>` | 高/中/低 | 高/中/低 | `<填写>` |
| `<填写>` | 高/中/低 | 高/中/低 | `<填写>` |

## 9. 结论
- POC 是否达到目标：`<填写>`
- 建议是否进入下一阶段：`建议进入 / 需补充验证 / 暂不建议`
- 决策说明：`<填写>`

## 10. 下一步计划（建议 2-4 周）
1. 补充压测与性能基线。
2. 增加监控告警（队列堆积、WS 连接、慢查询）。
3. 完善管理员端指标与审计视图。
4. 规划多节点部署与容灾演练。

## 附录
- 架构文档：`docs/support_system_architecture.md`
- 部署文档：`docs/deploy_single_host_ec2.md`
- 进度追踪：`docs/poc_progress.md`
- 计划文档：`docs/poc_plan.md`
