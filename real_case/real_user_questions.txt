Hi Team, I am new to Agora and trying to integrate Agora SDK. However, I don't know how to join the channel as requested. Could you help explain to me and guide me to join the user into the channel?
Hi, i want to know how to enable the dual stream
I got black screen, what should I do?
Hello, Agora team. We are using the Ban User Privileges API (POST /dev/v1/kicking-rule) to disband channels after a broadcast ends, but we have found some differences between the official documentation and the actual API behavior, so we would like to inquire about them. 1. uid: 0 cannot be used. According to the documentation (https://docs.agora.io/en/broadcast-streaming/channel-management-api/best-practices/ban-user-privileges#disband-a-channel), when targeting all users in a channel, it says to use uid: 0. However, in actual use: "uid": 0 (number) -> Error: uid '0' must be a number, or set str_uid = true. Omitting the uid field entirely works correctly. 2. Cannot create a permanent rule with time: 0. The documentation states that time: 0 means the rule is applied permanently. However, when we actually send time: 0, the API returns {{"status":"success","id":0}}, but when we query the rule list, no rule has been created. The rule is only created successfully when a positive value is used (for example, time: 5).
We have a multi-user setup on a streaming application running Unity Windows. Both users join the same channel. Uid 2 streams video and audio, uid 20 streams streams only audio from remote participants. Both are with role Host. All of that using custom audio and video frame pushing. Upon creation of such room, another Host joins (random uid). As long as he stays in the call - all is good. The moment he leaves - bitrate drops to 150kbps. And never recovers no matter what you do. Audience users can leave and enter with no problem. What should we do to keep bitrate stable? This is the way we do config: var videoConfig = new VideoEncoderConfiguration { dimensions = new VideoDimensions { width = config.broadcastResolution.width, height = config.broadcastResolution.height }, frameRate = config.frameRate, bitrate = config.bitrate, minBitrate = config.minBitrate, degradationPreference = config.degradation switch { DegradationPreference.MaintainFrameRate => DEGRADATION_PREFERENCE.MAINTAIN_FRAMERATE, DegradationPreference.MaintainResolution => DEGRADATION_PREFERENCE.MAINTAIN_RESOLUTION, DegradationPreference.Balanced => DEGRADATION_PREFERENCE.MAINTAIN_BALANCED, _ => DEGRADATION_PREFERENCE.DISABLED, }, codecType = config.codec switch { VideoCodec.H265 => VIDEO_CODEC_TYPE.VIDEO_CODEC_H265, _ => VIDEO_CODEC_TYPE.VIDEO_CODEC_H264, }, mirrorMode = VIDEO_MIRROR_MODE_TYPE.VIDEO_MIRROR_MODE_ENABLED, orientationMode = ORIENTATION_MODE.ORIENTATION_MODE_FIXED_LANDSCAPE, advanceOptions = new AdvanceOptions { encodingPreference = config.encodingPreference switch { EncodingPreference.Software => ENCODING_PREFERENCE.PREFER_SOFTWARE, _ => ENCODING_PREFERENCE.PREFER_HARDWARE, }, compressionPreference = config.compressionPreference switch { CompressionPreference.Quality => COMPRESSION_PREFERENCE.PREFER_QUALITY, _ => COMPRESSION_PREFERENCE.PREFER_LOW_LATENCY, }, }, }; Also tried these params: Resolution: 1920x1080 FPS: 60 Bitrate: 4300 kbps MinBitrate: 3000 kbps Degradation: DISABLED Codec: VIDEO_CODEC_H264 MirrorMode: VIDEO_MIRROR_MODE_ENABLED Orientation: ORIENTATION_MODE_FIXED_LANDSCAPE EncodingPreference: PREFER_HARDWARE CompressionPreference: PREFER_LOW_LATENCY It feels like min bitrate isn't respected at all. As feedback from OnLocalVideoStats is: [RtcEventHandler] OnLocalVideoStats UID: 2 SentBitrate: 117 kbps TargetBitrate: 4290 kbps SentFrameRate: 59 TargetFrameRate: 59 EncodedResolution: 1920x1080 EncoderOutputFrameRate: 59 RendererOutputFrameRate: 60 PacketLossRate: 0%
