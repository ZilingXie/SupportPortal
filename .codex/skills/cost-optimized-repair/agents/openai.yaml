interface:
  display_name: "Cost Optimized Repair"
  short_description: "Verify Claude Code, decompose work, dispatch agents"
  default_prompt: "Use $cost-optimized-repair to verify Claude Code, decompose the request into PR-sized slices and safe worker payloads, dispatch one or more Claude Code agents, and review the returned diffs."
