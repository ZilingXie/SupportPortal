interface:
  display_name: "Cost Optimized Repair"
  short_description: "Verify Claude Code, split repair work, review diffs"
  default_prompt: "Use $cost-optimized-repair to verify Claude Code, split this repair into PR-sized worker payloads, dispatch safe Claude Code agents, and review the returned diffs."
