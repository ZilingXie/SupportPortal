interface:
  display_name: "Control CC"
  short_description: "Codex orchestration for Claude Code implementation"
  default_prompt: "Use $control-cc for delegated code work: keep Codex as orchestrator, turn goals or PR slices into concise implementation plans, let Claude Code implement in task or candidate worktrees without budget caps, wait through runner heartbeat/retry monitoring, read review_packet.json before long logs or full diffs, assign quality_score out of 10, verify, and finalize each real PR slice to main."
