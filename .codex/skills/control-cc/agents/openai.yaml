interface:
  display_name: "Control CC"
  short_description: "Plan-driven Claude Code delegation for code work"
  default_prompt: "Use $control-cc for delegated code work: verify Claude Code, turn goals or PR slices into implementation plans, run Claude Code in task or detached candidate worktrees, review and integrate patches, verify, and finalize each real PR slice to main."
